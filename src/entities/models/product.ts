export type Product = {
    
    id ?: string,
        
    name ?: string,
    
    description ?: string,

    price ?: number,

    thumbnail_image ?: string,

    stock ?: number

    category_id ?: string

}